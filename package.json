{
  "name": "strategy",
  "version": "1.0.0",
  "description": "docker run \\ --name postgres \\ -e POSTGRES_USER=root -e POSTGRES_PASSWORD=root -e POSTGRES_DB=heros -p 5432:5432 -d  postgres \\",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^7.18.1",
    "pg-hstore": "^2.3.3"
  }
}
